# 执行摘要

**状态**: ✅ 核心发现已确认
**最后更新**: 2026-01-29

---

## 核心发现

### 1. 像素数据位置 ✅

```
r6 = 0x100000 + r5 × 4
像素数据从 r6 + 6 开始（跳过 6 字节元数据）
```

### 2. 编码判定机制 ✅

```
encoding = (metadata[0] >> 4) >= 0x8 ? 特殊编码 : 标准编码
```

| metadata[0] 高4位 | 编码类型 | 列数 |
|------------------|----------|------|
| 0x0-0x7 | 标准 | 15 |
| 0x8-0xF | 特殊 | 14 |

### 3. Unicode 到 r5 映射 ✅

```
r5 = Unicode - 偏移量
偏移量来自偏移表 @ 0x14AD6
```

### 4. 两条渲染路径的关系 ✅

| 路径 | 地址 | 状态 | 说明 |
|------|------|------|------|
| 主路径 | 0x2DB58 | ✅ 实际使用 | 所有字符都使用 |
| 0x2DC22 | 0x2DC22 | ❌ 死代码 | 不执行，会导致无效内存访问 |

---

## 渲染管线总览

```
Unicode 字符
    │
    ▼
[Unicode → r5 映射]
    │
    ▼
[设置渲染上下文]
    │
    ▼
[调用渲染函数 0x2D3C6]
    │
    ▼
[编码类型判定 @ 0x2DB12]
    │
    ├─ 标准 (15列) ──┐
    │                │
    └─ 特殊 (14列) ──┤
                     │
                     ▼
              [0x2DB58: 像素数据加载]
                     │
                     ▼
              [渲染到显示缓冲区]
```

---

## 已解决的问题

| 问题 | 状态 | 日期 |
|------|------|------|
| 像素数据位置 | ✅ | 2026-01-28 |
| 编码类型判定 | ✅ | 2026-01-28 |
| Unicode → r5 映射 | ✅ | 2026-01-28 |
| 0x2DC22 路径状态 | ✅ | 2026-01-28 |

---

## 待解决问题

| 问题 | 优先级 | 状态 | 更新 |
|------|--------|------|------|
| 元数据 [2], [4], [5] 功能 | 🟡 中 | ⚠️ 数据分析完成，用途待确认 | 2026-01-29 |
| 符号表具体用途 | 🟡 中 | ⚠️ ldrsb 用于设置标志位，动态存储用途未知 | 2026-01-29 |
| 14 列编码最高 2 位含义 | 🟡 中 | ⚠️ 待分析 | 2026-01-29 |
| Unicode 映射边界情况 | 🟢 低 | ⚠️ 待验证 | 2026-01-29 |

---

**参见**: [完整渲染管线](../02_ARCHITECTURE/RENDERING_PIPELINE.md)
